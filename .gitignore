.env
*.db
*.db-journal
*.sqlite3
__pycache__/
*.pyc
*.pyo
data/
.venv/
venv/
.DS_Store
**/.DS_Store

# Локальные приватные заметки (аудит, внутренняя структура) — не коммитить
AUDIT.local.md
*.local.md

# состояние дрифт-чекера (не коммитим, это runtime state, не код)
node/.drift_nudge_state.json
