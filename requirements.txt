aiogram==3.13.1
aiohttp==3.10.10
apscheduler==3.10.4
python-dotenv==1.0.1
# X25519 keypair generation для перегенерации REALITY-ключей (bot/reality_admin.py).
cryptography>=42,<45
# Бэкап в R2 (S3-совместимое). boto3 — обязателен для загрузки.
# pyrage (age-шифрование) ставится отдельно в Dockerfile как best-effort,
# чтобы отсутствие wheel под арх не валило сборку всего образа.
boto3>=1.34,<2
