aiogram==3.13.1
aiohttp==3.10.10
apscheduler==3.10.4
python-dotenv==1.0.1
# Бэкап в R2 (S3-совместимое). boto3 — обязателен для загрузки; pyrage —
# опциональное age-шифрование (импортируется лениво, без него бот не падает).
boto3>=1.34,<2
pyrage>=1.1,<2
